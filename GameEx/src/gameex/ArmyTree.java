package gameex;
import java.util.*;
import java.io.*;
public class ArmyTree {

    public ArmyTree(File entities)throws FileNotFoundException{
        Scanner read = new Scanner(entities);
        
    }
    
    private abstract class EntityNode<Type> implements Comparable<EntityNode>{
        
        protected 
        
        public boolean move(){
            
        }
        
        public int compareTo(EntityNode other){
            return 
        }
    }
}
